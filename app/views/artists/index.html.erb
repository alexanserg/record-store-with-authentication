
<h1>Artists</h1>

<ul>
  <% @artists.each do |artist| %>
    <li><%= link_to artist.name, artist_path(artist) %></li>
  <% end %>
</ul>

<p><%= link_to "Add an artist", new_artist_path(@artist) %></p>
